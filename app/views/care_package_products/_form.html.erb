<%= form_with(model: care_package_product) do |form| %>
  <% if care_package_product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(care_package_product.errors.count, "error") %> prohibited this care_package_product from being saved:</h2>

      <ul>
        <% care_package_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :care_package_id, style: "display: block" %>
    <%= form.text_field :care_package_id %>
  </div>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
