<%= form_with(model: care_package) do |form| %>
  <% if care_package.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(care_package.errors.count, "error") %> prohibited this care_package from being saved:</h2>

      <ul>
        <% care_package.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
